# Intake first (new app)

Director orchestrator. Skill: **`director-orchestrate`** (available after `sync-project-skills.sh`).

If `docs/INTAKE.md` has placeholders, follow `process/INTAKE.md`.

Do not analyze lab-app GitHub or `new-app.sh` errors until intake is done.

Ask only: (1) platform iOS / macOS / both, (2) app idea. Update `docs/INTAKE.md`, then bootstrap.
